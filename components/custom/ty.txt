

<section className="px-4 py-10 md:px-12 lg:px-20 bg-gray-50">
      <div className="max-w-7xl mx-auto">
        <h2 className="text-3xl font-bold text-center mb-10">Our Features</h2>
        <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {cards.map((card, idx) => (
            <div
              key={idx}
              className="bg-white shadow-xl rounded-2xl p-6 text-center hover:shadow-2xl transition duration-300"
            >
              <div className="w-20 h-20 mx-auto mb-4 relative">
                <Image src={card.image} alt={card.title} layout="fill" />
              </div>
              <h3 className="text-xl font-semibold mb-2">{card.title}</h3>
              <p className="text-gray-600 text-sm">{card.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>